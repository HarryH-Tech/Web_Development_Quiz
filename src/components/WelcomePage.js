import React from 'react';

const WecomePage = ({ startQuiz, beginQuiz }) => {

		
		return (
			<>
				<button onClick={beginQuiz}>
					Start Quiz
				</button>
			</>
		
		)
	};

export default WecomePage;


// 				<h1>Hi {currentUser.displayName}</h1>
